import './App.css';

function App() {
  return (
    <div className="app-container">
      <h1>My Clean React App</h1>
      <p>This is a simple UI without the React logo.</p>
      <button onClick={() => alert('Button clicked!')}>Click Me</button>
    </div>
  );
}

export default App;
